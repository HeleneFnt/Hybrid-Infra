#cloud-config
hostname: ${hostname}
ssh_pwauth: false
ssh_authorized_keys:
  - ${ssh_key}
users:
  - name: "monitor"
    sudo: "ALL=(ALL) NOPASSWD:ALL"
    groups: users, monitor
    home: /home/monitor
    shell: "/bin/bash"
    ssh_authorized_keys:

package_update: true
packages:
  - qemu-guest-agent
runcmd:
  - [ systemctl, enable, --now, qemu-guest-agent ]